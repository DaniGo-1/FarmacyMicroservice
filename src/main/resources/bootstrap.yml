spring:
  application:
    name : farmacy-microservice
  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      retry:
        initialInterval: 3000
        multiplier: 1.5
        maxInterval: 5000
        maxAttempts: 10